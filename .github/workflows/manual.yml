{
    "name": "build",
    "on": {
        "workflow_dispatch" : {}
    },
    "jobs": {
        "work": {
            "runs-on": "ubuntu-latest",
            "steps": [
                {
                    "uses": "actions/checkout@v4"
                },
                {
                    "name": "work",
                    "run": "bash -xeu ./runner/executed_from_ci.sh"
                }
            ]
        }
    }
}
